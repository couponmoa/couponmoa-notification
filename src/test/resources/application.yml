spring:
  mail:
    host: localhost
    port: 2525
    username: dummy
    password: dummy

  data:
    redis:
      host: localhost
      port: 6379

  cloud:
    aws:
      credentials:
        access-key: dummy
        secret-key: dummy
      region:
        static: ap-northeast-2
      sqs:
        enabled: false

  datasource:
    url: jdbc:h2:mem:testdb
    driver-class-name: org.h2.Driver
    username: sa
    password:

management:
  health:
    mail:
      enabled: false